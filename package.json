{
  "name": "sourcemap-fyi",
  "version": "1.0.0",
  "main": "index.js",
  "license": "MIT",
  "private": true,
  "scripts": {
    "start": "yarn workspace web run start",
    "dev": "yarn workspace web run dev",
    "build": "yarn workspace source-map-explorer build && yarn workspace web run build"
  },
  "workspaces": [
    "web",
    "packages/*"
  ],
  "dependencies": {
    "next": "^9.4.2"
  }
}
